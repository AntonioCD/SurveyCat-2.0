@page "/fichas/colindantes/details/{FichaId:int}"

@if (loading)
{
    <Loading />
}
else
{
    <MudTable Items="@colindantes"
              @ref="table"
              ServerData="LoadListAsync"
              Dense="true"
              Hover="true"
              Striped="true"
              FixedHeader="true"
              FixedFooter="true">
        <ToolBarContent>
            <MudText Typo="Typo.h6" Class="mr-4">COLINDANTES (@ficha?.CodEncuesta)</MudText>
            <MudButton Variant="Variant.Outlined" Class="mr-4" EndIcon="@Icons.Material.Filled.Add" Color="Color.Info" OnClick="@(() => RedirectToColindanteForm())">Crear</MudButton>
            <MudSpacer />
            <FilterComponent ApplyFilter="SetFilterValue" />
        </ToolBarContent>
        <HeaderContent>
            <MudTh Style="width: 20%;">Punto Cardinal</MudTh>
            <MudTh Style="width: 20%;">Nombre / Razon Social</MudTh>
            <MudTh Style="width: 20%;">Identificación</MudTh>
            <MudTh Style="width: 20%;">Tipo Identificación</MudTh>
            <MudTh Style="width: 20%;">¿Presenta Conflicto?</MudTh>
            <MudTh Style="width: 1%; white-space: nowrap; text-align: center;">Acciones</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Punto Cardinal">@context.PuntoCardinal?.Nombre</MudTd>
            <MudTd DataLabel="Nombre o Razón Social">@context.Persona?.NombreCompleto</MudTd>
            <MudTd DataLabel="Identificación">@context.Persona?.Identificacion</MudTd>
            <MudTd DataLabel="Tipo Identificación">@context.Persona?.TipoIdentificacion?.Nombre</MudTd>
            <MudTd DataLabel="¿Presenta Conflicto?">
                @(context.PresentaConflicto ? "Sí" : "No")
            </MudTd>
            <MudTd DataLabel="Acciones" Style="text-align: center; white-space: nowrap;">
                <div class="d-flex justify-center">
                    <MudTooltip Text="Editar">
                        <MudIconButton Icon="@Icons.Material.Filled.Edit"
                                       Variant="Variant.Outlined"
                                       Color="Color.Warning"
                                       OnClick="@(() => RedirectToColindanteForm(context.Id, true))"
                                       Size="Size.Medium"
                                       Class="mr-2" />
                    </MudTooltip>
                    <MudTooltip Text="Borrar">
                        <MudIconButton Icon="@Icons.Material.Filled.Delete"
                                       Variant="Variant.Outlined"
                                       Color="Color.Error"
                                       OnClick="@(() => DeleteAsync(context))"
                                       Size="Size.Medium" />
                    </MudTooltip>
                </div>
            </MudTd>
        </RowTemplate>
        <NoRecordsContent>
            <MudText>No se encontraron registros.</MudText>
        </NoRecordsContent>
        <PagerContent>
            <MudTablePager RowsPerPageString="Numero de Registros:"
                           PageSizeOptions="pageSizeOptions"
                           AllItemsText="Todos"
                           InfoFormat="@infoFormat" />
        </PagerContent>
    </MudTable>
}

