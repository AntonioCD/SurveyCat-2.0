<EditForm EditContext="editContext" OnValidSubmit="OnValidSubmit">
    <DataAnnotationsValidator />
    <MudTextField Label="Municipio"
                  @bind-Value="@Municipio.Nombre"
                  For="@(() => Municipio.Nombre)"
                  Required="true"
                  Variant="Variant.Outlined"
                  Margin="Margin.Dense"
                  Class="mb-4" />
    <MudTextField Label="Cod. SILEC"
                  @bind-Value="@Municipio.CodMuni"
                  For="@(() => Municipio.CodMuni)"
                  Required="true"
                  MaxLength="4"
                  Pattern="[0-9]*"
                  InputMode="InputMode.numeric"
                  Variant="Variant.Outlined"
                  Margin="Margin.Dense"
                  Class="mb-4" />
    <MudTextField Label="Cod. INIDE"
                  @bind-Value="@Municipio.CodINIDE"
                  For="@(() => Municipio.CodINIDE)"
                  MaxLength="4"
                  Pattern="[0-9]*"
                  InputMode="InputMode.numeric"
                  Variant="Variant.Outlined"
                  Margin="Margin.Dense"
                  Class="mb-4" />

    <MudButton Variant="Variant.Outlined"
               StartIcon="@Icons.Material.Filled.ArrowBack"
               Color="Color.Info"
               OnClick="ReturnAction">
        <MudHidden xs="true">
            Cancelar
        </MudHidden>
    </MudButton>

    <MudButton Variant="Variant.Outlined"
               StartIcon="@Icons.Material.Filled.Check"
               Color="Color.Primary"
               ButtonType="ButtonType.Submit">
        <MudHidden xs="true">
            Guardar
        </MudHidden>
    </MudButton>
</EditForm>