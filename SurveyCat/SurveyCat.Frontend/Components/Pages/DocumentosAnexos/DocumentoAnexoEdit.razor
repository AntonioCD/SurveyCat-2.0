@page "/documentosAnexos/edit/{Id:long}/{FichaId:int}/{CodEncuesta}"

@if (documentoAnexo is null)
{
    <Loading />
}
else
{
    <DocumentoAnexoForm CodEncuesta="@CodEncuesta" DocumentoAnexo="documentoAnexo" OnValidSubmit="EditAsync" ReturnAction="Return" />
}