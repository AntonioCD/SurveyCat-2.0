@using SurveyCat.Shared.Enums;
@using SurveyCat.Shared.Entities
@using System.ComponentModel.DataAnnotations

<EditForm EditContext="editContext" OnValidSubmit="OnValidSubmit">
    <DataAnnotationsValidator />

    <ValidationSummary />

    <MudGrid>
        <MudItem xs="12" Class="mb-2">
            @* d-flex y justify-space-between empujan el título a la izquierda y el input a la derecha *@
            <div class="d-flex align-center justify-space-between gap-3 pl-1">

                @* BLOQUE IZQUIERDO: Icono + Título y Subtítulo *@
                <div class="d-flex align-center gap-3">
                    <MudIcon Icon="@Icons.Material.Filled.Info" Color="Color.Primary" Size="Size.Large" />
                    <div class="d-flex flex-column">
                        <MudText Typo="Typo.h5" Color="Color.Primary">
                            <b>@(DocumentoAnexo?.Id != 0 ? "Editar Documento Anexo" : "Nuevo Documento Anexo")</b>
                        </MudText>
                        <MudText Typo="Typo.caption" Color="Color.Secondary">
                            Complete los datos requeridos para registrar/editar un documento anexo.
                        </MudText>
                    </div>
                </div>
            </div>
            <MudDivider Class="mt-3 mb-2" DividerType="DividerType.FullWidth" />
        </MudItem>

        <MudItem xs="12">
            <MudAutocomplete T="Diccionario"
                             Label="Tipo de Documento"
                             Placeholder="-- Seleccione un Documento --"
                             SearchFunc="SearchDocumento"
                             Value="selectedDocumento"
                             ValueChanged="DocumentoChanged"
                             Variant="Variant.Outlined" Margin="Margin.Dense"
                             ToStringFunc="@(e=> e==null?null : $"{e.Nombre}")">
                <ItemTemplate Context="itemContext">
                    @itemContext.Nombre
                </ItemTemplate>
            </MudAutocomplete>
        </MudItem>

        <MudItem xs="12">
            <MudTextField Label="Descripción" @bind-Value="DocumentoAnexo.Descripcion" Variant="Variant.Outlined" Margin="Margin.Dense" />
        </MudItem>

        <MudItem xs="12" sm="6">
            <MudNumericField Label="# Paginas" @bind-Value="DocumentoAnexo.NumeroPaginas"
                             Variant="Variant.Outlined" Margin="Margin.Dense" />
        </MudItem>

        <MudItem xs="12" sm="6">
            <MudTextField Label="Código" @bind-Value="DocumentoAnexo.Codigo" Variant="Variant.Outlined" Margin="Margin.Dense" />
        </MudItem>

        <MudItem xs="12" class="d-flex justify-end gap-2 mt-4">
            <MudButton Variant="Variant.Filled" Color="Color.Secondary" OnClick="ReturnAction">Cancelar</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Primary" ButtonType="ButtonType.Submit">Guardar</MudButton>
        </MudItem>

    </MudGrid>
</EditForm>